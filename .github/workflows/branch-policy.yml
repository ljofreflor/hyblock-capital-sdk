name: Branch Policy

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop ]
  create:
    ref_type: branch

jobs:
  enforce-branch-policy:
    name: Enforce Branch Policy
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'create'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Validate PR policies
        if: github.event_name == 'pull_request'
        env:
          HEAD_REF: ${{ github.head_ref }}
          BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          echo "🔍 Validando política de ramas..."
          echo "Base: $BASE_REF | Head: $HEAD_REF"
          
          # Validar PR a main solo desde develop
          if [ "$BASE_REF" = "main" ] && [ "$HEAD_REF" != "develop" ]; then
            echo "❌ [POLICY VIOLATION] Solo se permiten PRs a main desde la rama 'develop'."
            echo "❌ Rama origen: $HEAD_REF (debe ser 'develop')"
            echo "❌ Rama destino: $BASE_REF"
            exit 1
          fi
          
          # Validar PR a develop solo desde feature/fix branches
          if [ "$BASE_REF" = "develop" ]; then
            if [[ "$HEAD_REF" != feature/* && "$HEAD_REF" != fix/* && "$HEAD_REF" != hotfix/* ]]; then
              echo "❌ [POLICY VIOLATION] Solo se permiten PRs a develop desde ramas 'feature/*', 'fix/*' o 'hotfix/*'."
              echo "❌ Rama origen: $HEAD_REF (debe ser feature/*, fix/* o hotfix/*)"
              echo "❌ Rama destino: $BASE_REF"
              exit 1
            fi
          fi
          
          echo "✅ [POLICY] Regla de ramas cumplida - PR desde $HEAD_REF a $BASE_REF"
      
      - name: Block direct push to main branch only
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "❌ [POLICY VIOLATION] Push directo a main no permitido."
          echo "❌ Solo se permiten PRs desde develop que pasen los tests unitarios."
          echo "❌ Flujo: feature/* → develop → main"
          echo "❌ Usa: git push origin develop y crea un PR"
          exit 1
      
      - name: Setup Python and Poetry
        if: github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'develop')
        run: |
          echo "🐍 Configurando Python y Poetry..."
          
          # Verificar que Python esté disponible
          python --version || python3 --version
          
          # Instalar Poetry si no está disponible
          if ! command -v poetry &> /dev/null; then
            echo "📦 Instalando Poetry..."
            curl -sSL https://install.python-poetry.org | python3 -
            export PATH="$HOME/.local/bin:$PATH"
          fi
          
          # Verificar que Poetry esté disponible
          poetry --version
          
          echo "✅ Python y Poetry configurados correctamente"

      - name: Run unit tests for PR validation
        if: github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'develop')
        run: |
          echo "🧪 Ejecutando tests unitarios para validar PR..."
          
          # Asegurar que Poetry esté en el PATH
          export PATH="$HOME/.local/bin:$PATH"
          
          # Instalar dependencias
          poetry install --with dev
          
          # Ejecutar tests unitarios
          echo "🔍 Ejecutando tests unitarios..."
          poetry run pytest tests/ -v --tb=short
          
          echo "✅ Tests unitarios pasaron exitosamente"
      
      - name: Validate PR requirements
        if: github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'develop')
        run: |
          echo "📋 Validando requisitos del PR..."
          
          # Verificar que el PR tenga descripción
          if [ -z "${{ github.event.pull_request.body }}" ]; then
            echo "⚠️  Advertencia: El PR no tiene descripción"
          else
            echo "✅ PR tiene descripción"
          fi
          
          # Verificar que no sea un draft
          if [ "${{ github.event.pull_request.draft }}" = "true" ]; then
            echo "❌ [POLICY] No se permiten PRs en draft a ramas protegidas"
            exit 1
          fi
          
          echo "✅ PR cumple con los requisitos básicos"
      
      - name: Validate branch creation policy
        if: github.event_name == 'create' && github.event.ref_type == 'branch'
        env:
          NEW_BRANCH: ${{ github.event.ref }}
        run: |
          echo "🔍 Validando política de creación de ramas..."
          echo "Nueva rama: $NEW_BRANCH"
          
          # Verificar que no se creen ramas desde main
          if [ "$NEW_BRANCH" != "main" ] && [ "$NEW_BRANCH" != "develop" ]; then
            # Obtener información de la rama base
            BASE_BRANCH=$(git show-ref --heads | grep "$NEW_BRANCH" | head -1 | cut -d' ' -f2 | sed 's|refs/heads/||')
            
            # Verificar si la rama se creó desde main
            if git merge-base --is-ancestor main "$NEW_BRANCH" 2>/dev/null; then
              if ! git merge-base --is-ancestor develop "$NEW_BRANCH" 2>/dev/null; then
                echo "❌ [POLICY VIOLATION] No se permiten ramas creadas desde main."
                echo "❌ Rama creada: $NEW_BRANCH"
                echo "❌ Todas las ramas deben crearse desde develop."
                echo "❌ Usa: git checkout develop && git checkout -b $NEW_BRANCH"
                exit 1
              fi
            fi
          fi
          
          echo "✅ [POLICY] Creación de rama cumple con las reglas"


